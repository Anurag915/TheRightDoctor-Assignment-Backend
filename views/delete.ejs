<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Delete Person</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <div class="card shadow border-danger">
      <div class="card-header bg-danger text-white">
        <h3 class="mb-0">🗑️ Delete Confirmation</h3>
      </div>
      <div class="card-body">
        <p class="fs-5">Are you sure you want to delete <strong><%= person.name %></strong>?</p>
        <form action="/person/<%= person._id %>?_method=DELETE" method="POST" class="d-flex gap-3">
          <button type="submit" class="btn btn-danger">Yes, Delete</button>
          <a href="/person" class="btn btn-secondary">Cancel</a>
        </form>
      </div>
    </div>
  </div>
</body>
</html>
